# Expected SOAP Notes

This directory contains expected SOAP note outputs for test cases.

Files should be named `<case_id>.txt` matching the transcript file name.

Example:
- `otalgia_simple.txt` - Expected SOAP for the otalgia_simple test case
