name: scribe_eval_harness
description: Clinical evaluation harness for Scribe V2 pipeline
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.0
  path: ^1.8.0
  collection: ^1.17.0
  crypto: ^3.0.3
  # Use Dart-only runtime package instead of Flutter app
  docsoft_scribe_runtime:
    path: ../../packages/docsoft_scribe_runtime

dev_dependencies:
  test: ^1.24.0
  lints: ^2.1.0