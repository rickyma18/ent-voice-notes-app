import 'package:flutter/material.dart';

class MedicalNoteForm extends StatelessWidget {
  const MedicalNoteForm({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement MedicalNoteForm
    return const Column(
      children: [
        Text('Medical Note Form'),
      ],
    );
  }
}
