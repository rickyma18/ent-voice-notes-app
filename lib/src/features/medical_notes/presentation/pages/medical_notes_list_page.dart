import 'package:flutter/material.dart';

class MedicalNotesListPage extends StatelessWidget {
  const MedicalNotesListPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement MedicalNotesListPage
    return const Scaffold(
      body: Center(
        child: Text('Medical Notes List Page'),
      ),
    );
  }
}
