import 'package:flutter/material.dart';

class CreateMedicalNotePage extends StatelessWidget {
  const CreateMedicalNotePage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement CreateMedicalNotePage
    return const Scaffold(
      body: Center(
        child: Text('Create Medical Note Page'),
      ),
    );
  }
}
